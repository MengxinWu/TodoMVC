// global Backbone var
var app = app || {};
var ENTER_KEY = 13;
var ESC_KEY = 27;

$(function () {
	// use strict mode
	"use strict";

	// instantiate AppView to start the program
	new app.AppView();
});